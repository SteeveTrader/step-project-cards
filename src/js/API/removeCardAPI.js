// import instance from "./instance.js";
// import deleteCard from "../functions/removeCard.js";

// const removeCardAPI = async (body) => {
//   try {
//     return await instance.delete(`/{deleteObject.id}`, body);

//   } catch (err) {
//     console.log('Error', err);
//   }

// };

// export default removeCardAPI;


// import instance from "./instance.js";

// const removeCardAPI = async (id) => {
//   try {
//     // Обратите внимание, что вместо `/{deleteObject.id}` используется шаблонная строка `${id}` для подстановки значения id
//     return await instance.delete(`/${id}`);
//   } catch (err) {
//     console.log('Error', err);
//   }
// };

// export default removeCardAPI;
